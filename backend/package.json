{
  "name": "weathersphere-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "WeatherSphere API backend — rate limiting, caching, load balancing",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "build:frontend": "cd ../frontend && npm install && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "ioredis": "^5.7.0",
    "pino": "^9.8.0",
    "pino-pretty": "^13.0.0",
    "prom-client": "^15.1.3",
    "uuid": "^11.1.0"
  }
}
